[pytest]
addopts = --strict-markers
markers =
    unit: Fast deterministic tests without network or credentials.
    integration: Multi-component integration tests using local fixtures or mocked boundaries.
    live: Tests that call external APIs and require credentials.

